---
- name: test connectivity
  hosts: vm
  tasks:
    - name: ping hosts
      ansible.builtin.ping:
    - name: Create a directory if it does not exist
      ansible.builtin.file:
        path: /tmp/roshankhatri
        state: directory
        mode: '0755'